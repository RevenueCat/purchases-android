package com.revenuecat.purchases.ui.revenuecatui

import androidx.compose.runtime.Composable

/**
 * Composable offering a footer Paywall UI using a bottom sheet. It can be configured from the RevenueCat dashboard.
 * @param options The options to configure the PaywallFooter if needed.
 */
@Suppress("unused")
@Composable
fun PaywallFooter(options: PaywallFooterOptions = PaywallFooterOptions.Builder().build()) {
    // WIP
}

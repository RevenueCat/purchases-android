<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <application>
        <activity
                android:name="com.revenuecat.purchases.amazon.purchasing.ProxyAmazonBillingActivity"
                android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
                android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity
                android:name="com.revenuecat.purchases.SimulatedStoreErrorDialogActivity"
                android:theme="@android:style/Theme.Material.Dialog.Alert"
                android:excludeFromRecents="true"
                android:configChanges="orientation|screenSize" />
    </application>

</manifest>

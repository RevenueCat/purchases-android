package com.revenuecat.purchases.rules

/**
 * Public namespace for the RevenueCat rules engine.
 *
 * This module is an implementation detail of the RevenueCat SDK. It is published as
 * a separate Maven artifact so it can be consumed transitively by `:purchases` /
 * `:ui:revenuecatui`, but it is not intended for direct consumption by app
 * developers and offers no compatibility guarantees.
 */
public object RulesEngine

name: Claude Code Review

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

jobs:
  claude-review:
    uses: revenuecat/sdk-github-workflows/.github/workflows/claude-code-review.yml@b65ffeb823c070033f8e6a98680a6dbfdaec7636
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

